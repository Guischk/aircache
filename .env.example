# Airtable Configuration
AIRTABLE_PERSONAL_TOKEN = your-personal-token
AIRTABLE_BASE_ID = base-id

# SQLite configuration (replaces Redis)
SQLITE_PATH = data/aircache.db
CACHE_TTL = 86400 # 24 hours
REFRESH_INTERVAL = 86400 # 24 hours (daily refresh)

# Storage configuration for attachments
STORAGE_PATH = ./data/attachments

# Legacy Redis configuration (for migration/comparison)
# REDIS_URL = redis://yourcomplete.url

# Bearer token
# https://generate-random.org/api-token-generator
BEARER_TOKEN = votre-token-securisé

# API Server
PORT = 3000